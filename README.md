# Mack — Video & Graphic Portfolio

Local preview:
1. Start a static server: `python3 -m http.server 8000`
2. Open the site in host browser: `"$BROWSER" http://localhost:8000`

Replace assets in assets/ (thumbs, videos, graphics). Update data/projects.json to add entries.
